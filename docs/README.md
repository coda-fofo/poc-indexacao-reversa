# Documentação - PoC Indexação Reversa

- Pré-requisitos: Java +17;
- Para rodar o projeto, execute o seguinte comando no Git Bash:

```bash
./start.sh poc-indexacao-reversa
```

- Depois, basta acessar a `localhost:8080`.